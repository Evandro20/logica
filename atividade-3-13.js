var user = require('readline-sync')
var x = 1
while ( x <= 5){
    var numero = user.questionInt( " Insira o "+x+" numero: ")
    x++
    var impar =
    if ( numero % 2 != 0)
        console.log( impar)
}